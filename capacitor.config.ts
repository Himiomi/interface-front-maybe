import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.freyr',
  appName: 'freyr',
  webDir: 'dist/apps/classic-client',
  bundledWebRuntime: false
};

export default config;
